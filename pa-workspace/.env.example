# PA Workspace Configuration

# Server
PORT=3003
NODE_ENV=development

# Database
DATABASE_URL=file:./pa-workspace.db

# JWT — must match the app backend's JWT_SECRET so tokens are interchangeable
JWT_SECRET=your-shared-jwt-secret-here

# App backend URL (the parent app this module serves)
APP_API_URL=http://localhost:3001
# Service token for app backend API calls (service-to-service auth)
APP_SERVICE_TOKEN=

# Logging
LOG_LEVEL=info
LOG_TO_FILE=false
LOG_DIR=./logs

# Google Workspace (configured per-team via admin API, but can set defaults here)
# GOOGLE_SERVICE_ACCOUNT_JSON=  # Path to service account key file
# GOOGLE_ADMIN_EMAIL=           # Super admin email for domain-wide delegation
# GOOGLE_DOMAIN=                # PA domain (e.g. pa.company.com)

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001
