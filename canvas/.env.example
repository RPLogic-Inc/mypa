# Canvas Configuration
# These are compile-time variables — set them before building.

# Base domain for URL resolution (used for smart API routing)
# For Docker Compose: leave as localhost (nginx handles routing)
# For custom domain: set to your domain (e.g., myteam.example.com)
VITE_BASE_DOMAIN=localhost

# Instance mode: "team" (multi-user, default) or "personal" (single-user hub)
VITE_INSTANCE_MODE=team

# Override API URLs (optional — usually auto-detected from hostname)
# VITE_RELAY_URL=http://localhost:3002
# VITE_BACKEND_URL=http://localhost:3001/api
